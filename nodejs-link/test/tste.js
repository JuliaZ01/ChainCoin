   const params = {
       destaddress : "dsff",
       coins: 500,
       time: '20190415',
       nowcoins:0,
       now: true,
   }
   const initInput = {
        params : params,
   }
    console.log(initInput);